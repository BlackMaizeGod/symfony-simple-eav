<form method="post"
      action="{{ path('product_attribute_relation_delete', {'type': product_attribute_relation.attribute.type.type , 'id': product_attribute_relation.id, }) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_attribute_relation.id) }}">
    <button class="btn">Delete</button>
</form>
